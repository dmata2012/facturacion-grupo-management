{
  "name": "grupo-management-facturacion",
  "version": "1.0.0",
  "description": "Sistema Integral de Facturación — Grupo Management",
  "scripts": {
    "start": "node backend/server.js",
    "install-deps": "cd backend && npm install",
    "crm": "node \"CRM Despacho/dev-server.js\""
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
